/** Etiquetas de la sección de trabajo, compartidas por sus componentes. */

export interface WorkCopy {
  eyebrow: string;
  title: React.ReactNode;
  visit: string;
  view: string;
  prev: string;
  next: string;
  resultsLabel: string;
  figma: string;
  code: string;
  overview: string;
  close: string;
}
